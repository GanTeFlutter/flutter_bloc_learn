

Bloc Nedir?

BLoC = Business Logic Component

Flutter’da iş mantığını UI’dan ayırmak için kullanılan bir tasarım desenidir.

Amaç: UI sadece veriyi gösterir, iş mantığı (state değişimleri, eventler, hesaplamalar) ayrı bir yerde yönetilir.

Nasıl çalışır:

Event (Olay) → Kullanıcı bir şey yapar (butona basmak, form doldurmak)

Bloc / Cubit → Event’i alır, iş mantığını uygular, yeni state üretir

State (Durum) → UI bunu dinler ve güncellenir



===============================
🌟 Temel Bloc Widget’ları (101)
===============================

Bloc kullanımı başta karmaşık görünebilir, ama temelde 3 şey vardır:
1. Bloc’u sağlamak
2. State’i dinlemek ve UI’ı güncellemek
3. Gerekirse yan etkileri yönetmek

===============================
🔹 Bloc Sağlamak
===============================

- BlocProvider → Tek bir Bloc veya Cubit sağlar
- MultiBlocProvider → Birden fazla Bloc veya Cubit sağlar

Örnek:
BlocProvider(
  create: (_) => CounterCubit(),
  child: CounterPage(),
);

===============================
🔹 State Dinlemek / UI Güncellemek
===============================

- BlocBuilder → State değişince UI’ı yeniden oluşturur
- BlocListener → State değişimlerini dinler, yan etkiler yapar (Snackbar, Navigation)
- MultiBlocListener → Birden fazla BlocListener kullanmak için

Örnek:
BlocBuilder<CounterCubit, int>(
  builder: (context, state) => Text('$state'),
);

BlocListener<CounterCubit, int>(
  listener: (context, state) {
    if(state == 10) ScaffoldMessenger.of(context).showSnackBar(SnackBar(content: Text("10'a ulaştı!")));
  },
  child: Container(),
);

===============================
🔹 Hem Dinle, Hem UI Güncelle
===============================

- BlocConsumer → BlocBuilder + BlocListener tek widget içinde

Örnek:
BlocConsumer<CounterCubit, int>(
  listener: (context, state) { /* yan etkiler */ },
  builder: (context, state) => Text('$state'),
);

===============================
🔹 State’in Sadece Bir Parçasını Dinle
===============================

- BlocSelector → Performans için, sadece ilgilendiğin kısmı dinler

Örnek:
BlocSelector<CounterCubit, int, bool>(
  selector: (state) => state > 0,
  builder: (context, isPositive) => Text(isPositive ? 'Pozitif' : 'Negatif'),
);

===============================
🔹 Repository / Service Sağlamak
===============================

- RepositoryProvider → Servis veya repository sağlar
- MultiRepositoryProvider → Birden fazla repository sağlar

Örnek:
RepositoryProvider(
  create: (_) => AuthRepository(),
  child: MyApp(),
);

Not: İlerleyen kısmlarda örneklerle açıklancak 
